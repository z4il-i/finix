# `steam`

Community module for `steam`. Currently sets up the following: 

- video drivers
- `steam` package

Also allows for extra configuration like environment variables, package overrides, etc.
For games that need `steam-run` simply add this to your `environment.systemPackages` `config.programs.steam.package.run`
